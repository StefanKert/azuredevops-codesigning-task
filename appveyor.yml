version: 1.0.0.{build}
configuration: Release
environment:
  nodejs_version: "6"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

build:
  - ps: gulp package

artifacts:
  - path: '**\*.vsix'